# ifndef MINHASH_H
# define MINHASH_H
# include <bits/stdc++.h>
# define MAX_HASH 105
using namespace std;

void min_hash(double similarity_rate, int hash_number);
void get_sig(int hash_number, vector <int *> &dig);

# endif